## elasticsearch_exporter(OpenSearch용) 환경변수 예시
## - 실제 값은 secrets로 주입
##
## 기본은 OpenSearch Security(TLS) 환경을 가정하므로 https 사용.

OPENSEARCH_URI=https://10.10.21.50:9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=CHANGE_ME

